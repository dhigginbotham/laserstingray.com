extends ../layout

block content
  .container
    h1 
      p.pull-right.small #{moment(blog.created_date).format('MMMM Do YYYY, h:mm:ss a')} 
      a(href="#") !{marked(blog.title)}
    hr
    P !{marked(blog.body)}
